# Day 10 Notes

Today's focus was on memory elements and buffers. Implementations include a Block RAM, a Simple Dual Port RAM, and a parameterized FIFO. Testbenches are provided to verify read/write operations and full/empty flags.
