# Day 02 Notes: Mealy FSM Sequence Detection

Today's session focused on Sequence Detectors using Mealy FSMs. We specifically looked at detecting an overlapping '1001' sequence using D flip-flops to store state bits (Q1, Q0). The output is combinational, heavily dependent on the current input.