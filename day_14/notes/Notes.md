# Day 14 Notes

Today's topic was UART communication. Implementations include a UART receiver (uart_rx) and a UART transmitter (uart_tx) with configurable baud rate and parity generation/checking. A top-level module integrates these components.
