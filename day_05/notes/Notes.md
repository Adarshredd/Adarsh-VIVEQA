# Day 05 Notes: Arithmetic Logic Unit (ALU)

Today we designed an Arithmetic Logic Unit (ALU). The ALU acts as the brain of a CPU, executing mathematical and bitwise operations based on a control signal. We implemented a 4-bit ALU with 12 distinct operations and status flags (Zero and Negative).